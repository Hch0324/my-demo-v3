# 现代化管理后台系统

一个基于 Vue 3 + TypeScript + Vite + Element Plus 的现代化管理后台系统，支持主题切换、响应式设计和丰富的数据可视化功能。

## 功能特性

### 核心功能
- 🎨 **主题切换** - 支持浅色/深色主题，自定义主题变量
- 📱 **响应式设计** - 完美适配移动端和桌面端
- 🧩 **组件化架构** - 模块化设计，易于扩展和维护
- 🚀 **现代化技术栈** - Vue 3 + TypeScript + Vite
- 🔒 **权限管理** - 基于路由的权限控制，动态菜单生成
- 📊 **数据可视化** - 集成 ECharts，支持多种图表类型
- 💾 **Mock 数据支持** - 内置 Mock 数据，方便开发和测试

### 页面功能
- 📊 **仪表盘** - 数据统计卡片、折线图、饼图、柱状图
- 📋 **数据管理** - 表格展示、搜索筛选、增删改查
- 📈 **图表分析** - 多种图表类型、数据维度切换、时间范围筛选
- 📢 **通知管理** - 通知列表、通知详情
- 👤 **用户管理** - 用户列表、角色管理、权限分配
- 🔧 **系统设置** - 主题设置、布局配置
- 👤 **个人中心** - 用户信息、密码修改

### 技术特性
- ⚡ **高性能** - Vite 构建、代码分割、懒加载
- 🎯 **TypeScript** - 类型安全、开发体验好
- 📦 **组件库** - Element Plus 提供丰富组件
- 📊 **数据可视化** - ECharts 图表库支持
- 🎨 **CSS 预处理器** - SCSS 样式管理
- 🔧 **代码规范** - ESLint + Prettier 代码格式化
- 📝 **完整的类型定义** - 无 `any` 类型
- 🚦 **路由守卫** - 完整的路由权限控制

## 技术栈

| 技术 | 版本 | 说明 |
| --- | --- | --- |
| **前端框架** | Vue 3.3+ | 渐进式 JavaScript 框架 |
| **构建工具** | Vite 4+ | 下一代前端构建工具 |
| **编程语言** | TypeScript 5+ | 类型安全的 JavaScript 超集 |
| **UI 框架** | Element Plus 2+ | 基于 Vue 3 的桌面端组件库 |
| **图表库** | ECharts 5+ | 基于 JavaScript 的开源可视化图表库 |
| **状态管理** | Pinia 2+ | 轻量级状态管理库 |
| **路由管理** | Vue Router 4+ | Vue.js 官方路由 |
| **HTTP 客户端** | Axios 1.5+ | 基于 Promise 的 HTTP 客户端 |
| **CSS 预处理器** | SCSS 1.66+ | CSS 扩展语言 |
| **图标库** | @element-plus/icons-vue | Element Plus 官方图标库 |
| **工具库** | @vueuse/core | Vue Composition API 工具集 |
| **代码规范** | ESLint 8.47+ | 代码质量检查工具 |
| **代码格式化** | Prettier 3.0+ | 代码格式化工具 |

## 快速开始

### 环境要求

- Node.js >= 16.0.0
- npm >= 8.0.0 或 pnpm >= 7.0.0

### 安装依赖

```bash
# 使用 npm
npm install

# 或使用 pnpm（推荐）
pnpm install
```

### 开发环境运行

```bash
# 启动开发服务器
npm run dev

# 或使用 pnpm
pnpm dev
```

访问 http://localhost:5173 查看应用。

### 生产环境构建

```bash
# 构建生产版本
npm run build

# 或使用 pnpm
pnpm build
```

构建产物在 `dist` 目录中。

### 预览构建结果

```bash
# 预览生产构建
npm run preview

# 或使用 pnpm
pnpm preview
```

### 代码检查和类型检查

```bash
# 代码检查和自动修复
npm run lint

# 类型检查
npm run type-check

# 或使用 pnpm
pnpm lint
pnpm type-check
```

## 项目结构

```
src/
├── api/                    # API 接口定义
│   ├── dashboard.ts       # 仪表盘相关 API
│   ├── index.ts           # API 入口文件
│   ├── notice.ts          # 通知相关 API
│   ├── table.ts           # 表格数据相关 API
│   └── user.ts            # 用户相关 API
├── assets/                 # 静态资源
│   ├── logo.svg           # Logo 文件
│   └── logo-mini.svg      # 小尺寸 Logo
├── config/                 # 配置文件
│   ├── index.ts           # 应用配置
│   └── menu.ts            # 菜单配置
├── layout/                 # 布局组件
│   ├── components/        # 布局相关组件
│   │   ├── Breadcrumb.vue # 面包屑导航
│   │   ├── Header.vue     # 顶部导航
│   │   └── Sidebar.vue    # 侧边栏
│   └── index.vue          # 主布局组件
├── router/                 # 路由配置
│   └── index.ts           # 路由定义
├── store/                  # 状态管理
│   ├── theme.ts           # 主题状态
│   └── user.ts            # 用户状态
├── styles/                 # 样式文件
│   ├── element-variables.scss # Element Plus 变量
│   ├── index.scss         # 全局样式
│   └── variables.scss     # 样式变量
├── types/                  # TypeScript 类型定义
│   ├── index.ts           # 全局类型
│   ├── mock.ts            # Mock 数据类型
│   └── user.ts            # 用户相关类型
├── utils/                  # 工具函数
│   ├── auth.ts            # 认证相关
│   ├── mock.ts            # Mock 数据生成
│   └── request.ts         # HTTP 请求封装
├── views/                  # 页面组件
│   ├── ChartAnalysis/     # 图表分析页面
│   │   └── index.vue
│   ├── Dashboard/         # 仪表盘页面
│   │   └── index.vue
│   ├── DataManage/        # 数据管理页面
│   │   ├── FormPage.vue   # 表单页面
│   │   ├── TableList.vue  # 表格列表
│   │   └── index.vue
│   ├── Error/             # 错误页面
│   │   └── 404.vue
│   ├── Login/             # 登录页面
│   │   └── index.vue
│   ├── Notice/            # 通知管理
│   │   ├── NoticeList.vue # 通知列表
│   │   └── index.vue
│   ├── System/            # 系统管理
│   │   ├── RoleManage.vue # 角色管理
│   │   ├── Settings.vue   # 系统设置
│   │   └── UserManage.vue # 用户管理
│   └── profile/           # 个人中心
│       └── index.vue
├── App.vue                 # 根组件
└── main.ts                 # 应用入口
```

## 核心功能详解

### 主题系统

项目支持完整的主题切换功能：

- **浅色主题** - 默认主题，清新简洁
- **深色主题** - 护眼模式，适合夜间使用
- **自定义主题** - 支持自定义主题变量

主题配置在 `src/styles/variables.scss` 中定义，包括：
- 主色调、辅助色调
- 背景色、文字色
- 边框色、阴影效果
- 动画过渡效果

主题切换逻辑在 `src/store/theme.ts` 中实现，通过修改 CSS 变量实现主题切换。

### 响应式布局

项目采用移动端优先的响应式设计：

- **移动端** (< 768px) - 侧边栏抽屉式，简化界面
- **平板端** (768px - 1024px) - 适配中等屏幕
- **桌面端** (> 1024px) - 完整功能展示

响应式断点和工具类在样式文件中定义，使用 CSS 媒体查询实现不同屏幕尺寸的适配。

### 路由和权限管理

基于 Vue Router 实现的权限管理：

1. **路由定义** - 在 `src/router/index.ts` 中定义路由，并标记需要权限的路由
2. **路由守卫** - 实现登录验证和权限检查
3. **动态菜单** - 根据用户权限动态生成侧边栏菜单
4. **权限控制** - 基于角色的权限分配

### 数据可视化

集成 ECharts 图表库，支持多种图表类型：

- **折线图** - 趋势分析
- **柱状图** - 对比分析  
- **饼图** - 占比分析
- **散点图** - 关联分析
- **仪表盘** - 性能指标

图表组件支持主题适配和响应式缩放，在 `src/views/ChartAnalysis` 和 `src/views/Dashboard` 中使用。

### Mock 数据支持

项目内置 Mock 数据支持，方便开发和测试：

- Mock 数据配置在 `src/utils/mock.ts` 中
- API 接口在 `src/api/` 目录中定义
- Mock 数据生成规则在 `src/types/mock.ts` 中定义

详细的 Mock 数据使用指南请查看 [MOCK_DATA_GUIDE.md](MOCK_DATA_GUIDE.md)。

## 开发指南

### 代码规范

项目遵循严格的代码规范：

- **命名规范** - 语义化命名，避免缩写
  - 变量：小驼峰命名，如 `userName`、`orderList`
  - 函数：小驼峰命名，动词开头，如 `getUserInfo()`、`handleSubmit()`
  - 组件：帕斯卡命名，如 `UserCard`、`OrderList`
  - 常量：全大写+下划线分隔，如 `MAX_RETRY_COUNT`
- **文件结构** - 模块化组织，职责单一
- **TypeScript** - 完整的类型定义，无 `any` 类型
- **注释规范** - 必要的代码注释，说明复杂逻辑

### 组件开发

创建新组件的建议：

1. 放在合适的目录中
2. 使用 TypeScript 定义 Props
3. 添加必要的注释说明
4. 考虑主题和响应式适配
5. 遵循组件化设计原则

示例组件结构：
```vue
<template>
  <div class="component-name">
    <!-- 组件内容 -->
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'

interface ComponentProps {
  title: string
  data?: any[]
}

const props = withDefaults(defineProps<ComponentProps>(), {
  data: () => []
})

// 组件逻辑
const handleClick = () => {
  // 处理点击事件
}
</script>

<style scoped lang="scss">
.component-name {
  /* 组件样式 */
}
</style>
```

### API 接口开发

API 接口统一管理在 `src/api/` 目录：

1. **模块化** - 按功能模块组织，如 `user.ts`、`dashboard.ts`
2. **类型安全** - 使用 TypeScript 定义请求和响应类型
3. **统一封装** - 使用 `request.ts` 封装 HTTP 请求
4. **错误处理** - 统一的错误处理机制

示例 API 定义：
```typescript
// src/api/user.ts
import request from '../utils/request';
import type { UserInfo, UserListParams, UserListResult } from '../types/user';

/**
 * 获取用户列表
 */
export const getUserList = (params: UserListParams): Promise<UserListResult> => {
  return request.get('/user/list', { params });
};

/**
 * 获取用户信息
 */
export const getUserInfo = (id: string): Promise<UserInfo> => {
  return request.get(`/user/${id}`);
};
```

### 状态管理

使用 Pinia 进行状态管理：

1. **模块化** - 按功能拆分状态模块，如 `user.ts`、`theme.ts`
2. **类型安全** - 完整的 TypeScript 支持
3. **持久化** - 支持状态持久化存储

示例状态定义：
```typescript
// src/store/user.ts
import { defineStore } from 'pinia';
import type { UserInfo } from '../types/user';

export const useUserStore = defineStore('user', {
  state: () => ({
    userInfo: null as UserInfo | null,
    token: '',
    roles: [] as string[]
  }),
  
  actions: {
    /**
     * 登录
     */
    login(token: string) {
      this.token = token;
      // 保存到 localStorage
      localStorage.setItem('token', token);
    },
    
    /**
     * 退出登录
     */
    logout() {
      this.token = '';
      this.userInfo = null;
      this.roles = [];
      // 清除 localStorage
      localStorage.removeItem('token');
    }
  }
});
```

## 部署指南

### 环境变量

项目支持环境变量配置：

```bash
# .env.development
VITE_API_BASE_URL=http://localhost:8080/api
VITE_APP_TITLE=管理后台系统

# .env.production
VITE_API_BASE_URL=https://api.example.com/api
VITE_APP_TITLE=管理后台系统
```

### 构建配置

Vite 构建配置在 `vite.config.ts` 中：

- **路径别名** - `@` 指向 `src` 目录
- **代理配置** - 开发环境 API 代理
- **优化配置** - 代码分割、压缩等

### 部署方式

#### 静态部署

1. 构建生产版本：
```bash
npm run build
```

2. 将 `dist` 目录下的文件部署到静态服务器（如 Nginx、Apache、Caddy 等）。

#### Nginx 配置示例

```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/dist;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location /api {
        proxy_pass http://your-api-server.com;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

#### Docker 部署

1. 创建 Dockerfile：
```dockerfile
FROM nginx:alpine
COPY dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
```

2. 创建 nginx.conf：
```nginx
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

3. 构建并运行 Docker 容器：
```bash
docker build -t vue-admin-system .
docker run -d -p 80:80 vue-admin-system
```

### 性能优化

项目内置多种性能优化：

- **代码分割** - 路由懒加载
- **图片优化** - 自动压缩和格式转换
- **缓存策略** - 合理的缓存配置
- **CDN 加速** - 静态资源 CDN 部署

## 常见问题

### Q: 如何添加新的路由和页面？

A: 1. 在 `src/views/` 目录下创建新的页面组件
   2. 在 `src/router/index.ts` 中定义新的路由
   3. 在 `src/config/menu.ts` 中添加新的菜单配置
   4. （可选）在 `src/api/` 目录中添加对应的 API 接口

### Q: 如何添加新的主题？

A: 1. 在 `src/styles/variables.scss` 中添加新的主题变量
   2. 在 `src/store/theme.ts` 中添加主题切换逻辑
   3. 在 `src/layout/components/Header.vue` 中添加主题切换按钮

### Q: 如何使用 Mock 数据？

A: 1. 在 `src/types/mock.ts` 中定义 Mock 数据类型
   2. 在 `src/utils/mock.ts` 中配置 Mock 数据生成规则
   3. 在 `src/api/` 目录中定义对应的 API 接口
   4. 开发环境下会自动使用 Mock 数据

### Q: 如何修改 API 基础地址？

A: 在 `.env.development` 或 `.env.production` 文件中修改 `VITE_API_BASE_URL` 环境变量。

### Q: 如何添加新的图表？

A: 1. 在 `src/views/ChartAnalysis` 或其他页面中导入 ECharts
   2. 配置图表选项
   3. 绑定到 DOM 元素
   4. 实现图表响应式

## 贡献指南

欢迎提交 Issue 和 Pull Request 来改进项目。

### 提交规范

请遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

```
<类型>[可选作用域]: <描述>

[可选正文]

[可选脚注]
```

**类型**：
- `feat`：新功能
- `fix`：bug 修复
- `docs`：文档更新
- `style`：代码格式调整（无逻辑变更）
- `refactor`：代码重构（无功能变更）
- `test`：添加/修改测试用例
- `chore`：依赖更新、构建配置变更

### 开发流程

1. Fork 仓库
2. 创建特性分支：`git checkout -b feature/xxx`
3. 提交代码：`git commit -m "feat: 新增 xxx 功能"`
4. 推送分支：`git push origin feature/xxx`
5. 创建 Pull Request

## 许可证

MIT License - 详见 [LICENSE](LICENSE) 文件

## 联系方式

如有问题或建议，欢迎通过以下方式联系：

- 提交 Issue
- 发送邮件
- 项目讨论区

---

** Enjoy coding! 🚀 **